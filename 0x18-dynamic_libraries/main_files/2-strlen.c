#include <string.h>
#include "main.h"

/**
 * _strlen - return the length of a string
 *
 * @s: a pointer parameter
 *
 * Return: int
 */
int _strlen(char *s)
{
	int len;

	len = strlen(s);
	return (len);
}
