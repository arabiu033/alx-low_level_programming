#include <string.h>
#include "main.h"

/**
 * _strcat - append a string2 to string1
 * @dest: string to make the change on
 * @src: string to be appended
 * Return: a pointer
 */
char *_strcat(char *dest, char *src)
{
	return (strcat(dest, src));
}
