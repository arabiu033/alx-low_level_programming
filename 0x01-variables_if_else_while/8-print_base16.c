#include <stdio.h>

/**
 * main - starting point
 *
 * Return: 0 success
 */
int main(void)
{
	int a;
	int b;

	for (a = 47; a < 58; a++)
		putchar(a);
	for (b = 97; b < 112; b++)
		putchar(b);
	putchar('\n');

	return (0);
}
